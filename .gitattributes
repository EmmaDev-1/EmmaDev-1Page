# Normalise line endings: the working tree may be CRLF on Windows, but the
# repository and CI (Linux) always see LF.
* text=auto eol=lf

# Binary assets must never be touched.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.mp4 binary
*.webm binary
*.pdf binary
*.ico binary
